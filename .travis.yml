language: node_js
cache:
  yarn: true
  directories:
    - node_modules
os:
  - linux
  - osx
script:
  - yarn test
  - yarn build
  - yarn bundlesize
before_script:
  - yarn cache clean
after_success:
  - yarn check-coverage
